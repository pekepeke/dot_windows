{
    "flutter": {
        "url": "https://storage.googleapis.com/flutter_infra_release/releases/stable/windows/flutter_windows_3.32.2-stable.zip",
        "after": "binPath = Join-Path Get-Location 'bin'; Move-Item -Path (Join-Path Get-Location 'flutter\\*') -Destination Get-Location ; env:Path.Split(';').Contains(binPath) -or (Set-Item -Path env:Path -Value (Join-Path env:Path binPath))"
    }
}